# --- Serveur BFF ---
PORT=8787
# Origine autorisée (le front Vite en dev)
ALLOWED_ORIGIN=http://localhost:5173

# --- Firebase (config web publique, sert au login email/mot de passe via l'API REST) ---
FIREBASE_WEB_API_KEY=AIzaSyBtNncAMPRaja95VUpbox0P5cRcq9vo0ro
FIREBASE_PROJECT_ID=solarcell-installator
# Compte de service Firebase Admin (JSON) — requis UNIQUEMENT pour /auth/firebase/session
# (vérification d'un idToken fourni par le client). Laisser vide sinon.
# GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# --- Odoo (VPS) — SECRETS, ne jamais committer ---
ODOO_URL=https://ton-odoo.exemple.com
ODOO_DB=ta_base_odoo
ODOO_API_KEY=CHANGE_ME_meme_valeur_que_solarcell.internal_api_key

# --- Google OAuth server-side (optionnel, pour activer le bouton "Continuer avec Google ID") ---
# GOOGLE_OAUTH_CLIENT_ID=
# GOOGLE_OAUTH_CLIENT_SECRET=
# GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8787/api/auth/google/callback
